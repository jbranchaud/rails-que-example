# rails-que-example

> An example of using Que to schedule jobs in a Rails app

## Setup

```bash
$ bundle install
$ rake db:create
$ rake db:migrate
```

## Run

```
$ rails s
```

Open up `localhost:3000`
